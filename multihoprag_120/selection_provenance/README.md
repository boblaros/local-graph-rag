# Selection provenance

The two JSONL files in this directory record the earlier sample excluded when
constructing the fixed 120-question dataset. They are inputs to the deterministic
subset builder and are retained only to reproduce the final selection audit.
