# Model identities and LightRAG settings are stored in the YAML configuration.

# Local Ollama endpoint.
OLLAMA_HOST=http://localhost:11434

# Maximum extraction input accepted by LightRAG.
MAX_EXTRACT_INPUT_TOKENS=8192

# Workspace paths are set by the run configuration.
